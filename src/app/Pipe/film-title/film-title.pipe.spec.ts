import { FilmTitlePipe } from './film-title.pipe';

describe('FilmTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new FilmTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
